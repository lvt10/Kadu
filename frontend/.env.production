# ─── Frontend — Variáveis de Ambiente ────────────────────────────────────────
# Todas as variáveis VITE_ são embutidas no build em tempo de compilação.

# URL da API — aponta para o backend
# Produção: substitua pelo domínio do seu Azure App Service
VITE_API_URL=https://SEU-BACKEND.azurewebsites.net/api
